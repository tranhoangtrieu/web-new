{
  "name": "nuxt-app",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "rm -rf .nuxt && nuxt build",
    "dev": "nuxt dev",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "postinstall": "npm install && nuxt prepare",
    "deploy": "gh-pages -d dist"
  },
  "dependencies": {
    "aos": "^2.3.4",
    "nuxt": "^3.13.0",
    "vue": "latest",
    "vue-router": "latest"
  },
  "devDependencies": {
    "gh-pages": "^6.1.1"
  },
  "engines": {
    "node": "20.x"
  }
}
